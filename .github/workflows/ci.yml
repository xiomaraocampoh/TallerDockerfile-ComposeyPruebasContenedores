name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Instalar dependencias
        run: uv sync

      - name: Tests unitarios
        run: uv run pytest --cov=src --cov-report=term --cov-fail-under=80 test/ -v

      - name: Tests BDD
        run: uv run behave

      - name: API y pruebas de carga
        run: |
          uv run uvicorn src.api:app --host 127.0.0.1 --port 8000 &
          sleep 3
          curl -sf http://127.0.0.1:8000/health || { echo "La API no respondió"; exit 1; }
          uv run locust -f locustfile.py --headless -u 30 -r 10 -t 20s --host http://127.0.0.1:8000
